apiVersion: v2
name: catalog
description: Device registry — the only service that holds a connection to the devices database.
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: platform-common
    version: 1.0.0
    repository: file://../platform-common
