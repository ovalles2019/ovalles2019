apiVersion: v2
name: scorer
description: CPU-bound anomaly scoring — the platform's HPA target and the one workload whose replica count should track load.
type: application
version: 1.0.0
appVersion: "1.0.0"
dependencies:
  - name: platform-common
    version: 1.0.0
    repository: file://../platform-common
